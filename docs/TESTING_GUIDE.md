# 🧪 Руководство по тестированию - AI Business Assistant

**Последнее обновление:** После исправления зависаний при запуске

---

## 📋 Что было исправлено

### 1. Улучшенное логирование запуска ✅
- Добавлены индикаторы прогресса [1/6] через [6/6]
- Подробные сообщения на каждом этапе инициализации
- Легко определить где именно происходит зависание

### 2. Таймауты для баз данных ✅
- Таймаут подключения: 10 секунд
- Таймаут запросов: 30 секунд
- Проверка соединений перед использованием (pool_pre_ping)
- Пул соединений оптимизирован

### 3. Оптимизированная обработка ошибок ✅
- Трассировка стека для всех исключений
- Понятные сообщения об ошибках
- Graceful degradation при недоступности сервисов

---

## 🚀 Как протестировать исправления

### Шаг 1: Обновить код

```powershell
# Получить последние изменения
git pull origin claude/top-10-improvements-011CUxwbw7GDrXAVtXyVZ5Yz

# Убедиться что зависимости установлены
pip install -r requirements.txt
```

### Шаг 2: Проверить сервисы

**PostgreSQL:**
```powershell
# Проверить что PostgreSQL запущен
docker-compose ps

# Если не запущен:
docker-compose up -d
```

**Redis:**
```powershell
# Проверить Redis
redis-cli ping
# Должен вернуть: PONG
```

### Шаг 3: Запустить бота с новым логированием

```powershell
# Активировать venv
.venv\Scripts\activate

# Запустить бота
python main.py
```

### Шаг 4: Что вы должны увидеть

**Нормальный запуск (успешный):**
```
============================================================
🤖 AI Business Assistant Starting...
============================================================

[1/6] Loading environment variables...
✅ Environment loaded

[2/6] Initializing database...
Инициализация базы данных...
База данных успешно инициализирована.
✅ Database ready

[3/6] Running database migrations...
✅ Field 'language' already exists in users table
✅ Migrations completed

[4/6] Initializing AI model...
   Configuring Gemini API...
   Loading model: gemini-pro-latest...
✅ AI model ready: gemini-pro-latest

[5/6] Configuring Telegram bot...
   Building bot application...
✅ Bot application configured

[6/6] Registering handlers...
   - Error handler
   - Command handlers (/start, /mydocs, /stats, /clear)
   - Inline button handler
   - Document handler
   - Audio/voice handler
   - Text message handler
✅ All handlers registered

============================================================
✅ Бот успешно запущен!
Бот готов к работе. Нажмите Ctrl+C для остановки.
============================================================
```

### Шаг 5: Определить проблему (если всё ещё зависает)

**Если бот зависает на [2/6] - База данных:**
```powershell
# Проверить что PostgreSQL доступен
psql -U ai_bot_user -d ai_bot_db -c "SELECT 1;"

# Если ошибка, проверить .env:
type .env | Select-String "DB_"
```

**Если бот зависает на [4/6] - AI модель:**
```powershell
# Проверить GEMINI_API_KEY
type .env | Select-String "GEMINI"

# Проверить доступность Google AI API
curl https://generativelanguage.googleapis.com/v1/models
```

**Если бот зависает на [5/6] - Telegram:**
```powershell
# Проверить TELEGRAM_BOT_TOKEN
type .env | Select-String "TELEGRAM"

# Проверить токен вручную:
# https://api.telegram.org/bot<YOUR_TOKEN>/getMe
```

---

## ✅ Базовые тесты функциональности

После успешного запуска протестируйте основные функции:

### 1. Команда /start
```
Отправить: /start
Ожидается: Приветственное сообщение с меню
```

### 2. Загрузка документа
```
Действие: Отправить PDF файл боту
Ожидается: "✅ Документ успешно обработан!"
```

### 3. AI Chat
```
Отправить: Привет, как дела?
Ожидается: Ответ от AI
```

### 4. Команда /mydocs
```
Отправить: /mydocs
Ожидается: Список загруженных документов
```

### 5. Команда /stats
```
Отправить: /stats
Ожидается: Статистика использования
```

---

## 🔍 Проверка производительности

### Время запуска
Бот должен запускаться за **5-15 секунд**:
- [1/6] ~ 1 сек
- [2/6] ~ 2-3 сек (первое подключение к БД)
- [3/6] ~ 1-2 сек
- [4/6] ~ 2-5 сек (загрузка AI модели)
- [5/6] ~ 1 сек
- [6/6] ~ 1 сек

**Если какой-то шаг занимает >30 секунд - это проблема!**

### Время ответа AI
```
Первый запрос: 2-5 секунд (без кэша)
Повторный запрос: <100ms (с кэшем)
```

### Использование памяти
```powershell
# Проверить использование памяти
# Windows PowerShell:
Get-Process python | Select-Object ProcessName, WS
```

Ожидается: 100-300 MB

---

## 🐛 Типичные ошибки и решения

### Ошибка 1: Таймаут подключения к БД
```
ERROR: could not connect to server: Connection timed out
```

**Решение:**
1. Проверить что PostgreSQL запущен: `docker-compose ps`
2. Проверить .env переменные: `DB_HOST`, `DB_PORT`
3. Проверить файрволл/антивирус

### Ошибка 2: GEMINI_API_KEY не найден
```
❌ ERROR: GEMINI_API_KEY not found in .env file!
```

**Решение:**
1. Открыть .env: `notepad .env`
2. Добавить: `GEMINI_API_KEY=ваш_ключ`
3. Получить ключ: https://makersuite.google.com/

### Ошибка 3: Неверный Telegram токен
```
telegram.error.InvalidToken: Invalid token
```

**Решение:**
1. Проверить токен в .env
2. Получить новый у @BotFather
3. Проверить что нет лишних пробелов

### Ошибка 4: Redis недоступен
```
ConnectionRefusedError: [Errno 111] Connection refused
```

**Решение:**
1. Запустить Redis: `docker-compose up -d`
2. Проверить: `redis-cli ping`
3. Проверить `REDIS_URL` в .env

---

## 📊 Расширенное тестирование

### Нагрузочное тестирование
```python
# Отправить 10 сообщений подряд
# Бот должен обработать все без ошибок
```

### Тест rate limiting
```python
# Отправить >20 запросов в минуту
# Ожидается: "⏳ Слишком много запросов. Подождите немного."
```

### Тест кэширования
```python
# Отправить один и тот же вопрос дважды
# Второй ответ должен быть мгновенным (<100ms)
```

### Тест обработки ошибок
```python
# Отправить некорректный файл (например .exe)
# Ожидается: "⚠️ Неподдерживаемый тип файла"
```

---

## 📈 Мониторинг

### Логи
```powershell
# Смотреть логи в реальном времени
# Просто запустите: python main.py
# Все логи выводятся в консоль
```

### База данных
```sql
-- Проверить количество пользователей
SELECT COUNT(*) FROM users;

-- Проверить количество документов
SELECT COUNT(*) FROM documents;
```

### Redis кэш
```powershell
# Статистика кэша
redis-cli DBSIZE

# Очистить кэш (при необходимости)
redis-cli FLUSHALL
```

---

## ✅ Чек-лист перед деплоем

- [ ] Бот запускается без ошибок
- [ ] Все 6 шагов инициализации завершаются успешно
- [ ] /start работает
- [ ] Загрузка документов работает
- [ ] AI ответы работают
- [ ] Кэширование работает
- [ ] Rate limiting работает
- [ ] База данных сохраняет данные
- [ ] Логи информативные
- [ ] Нет memory leaks после 1 часа работы

---

## 🚀 Следующие шаги

После успешного тестирования:

1. **Staging deployment** - Разверните на тестовом сервере
2. **End-to-End тесты** - Протестируйте все функции в staging
3. **Load testing** - Проверьте под нагрузкой
4. **Security audit** - Проверьте безопасность
5. **Production deployment** - Следуйте DEPLOYMENT.md

---

## 📞 Помощь

Если проблемы не решаются:

1. **Соберите информацию:**
   - Полный вывод `python main.py`
   - На каком шаге [X/6] зависает
   - Версия Python: `python --version`
   - ОС: `systeminfo | findstr OS`

2. **Проверьте логи:**
   - Вывод в консоли
   - `docker-compose logs` для PostgreSQL/Redis

3. **Проверьте .env:**
   - Все ли ключи заполнены?
   - Нет ли опечаток?

---

**Удачи с тестированием! 🎉**
