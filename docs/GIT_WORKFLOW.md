# 📥 Git Workflow в VS Code - Пошаговая инструкция

## 🔄 Как подтягивать обновления после каждого коммита

### Вариант 1: Через интерфейс VS Code (РЕКОМЕНДУЕТСЯ)

#### Шаг 1: Откройте Source Control (Git панель)
```
Ctrl + Shift + G  (или нажмите иконку веточки слева)
```

#### Шаг 2: Подтяните изменения
Вы увидите одну из этих кнопок:
- **"Sync Changes"** (стрелки вверх/вниз) - если есть изменения для синхронизации
- **"Pull"** (стрелка вниз) - если нужно только скачать
- **"Push"** (стрелка вверх) - если нужно только загрузить

**Нажмите "Sync Changes"** или **"Pull"**

#### Шаг 3: Разрешите конфликты (если есть)
Если VS Code показывает конфликты:
1. Откройте файл с конфликтом
2. Увидите кнопки:
   - "Accept Current Change" (ваша версия)
   - "Accept Incoming Change" (новая версия)
   - "Accept Both Changes" (объединить)
3. Выберите нужное

---

### Вариант 2: Через терминал VS Code (ДЛЯ ПРОФИ)

#### Откройте терминал в VS Code:
```
Ctrl + ` (обратная кавычка)
```

#### Команды Git:

```powershell
# 1. Проверить текущее состояние
git status

# 2. Подтянуть изменения с сервера
git pull origin claude/ai-business-intelligence-agent-011CUvANvRyXyYWG58FytrG8

# Если есть локальные изменения, сначала:
git stash                    # Спрятать локальные изменения
git pull origin <branch>     # Подтянуть обновления
git stash pop                # Вернуть локальные изменения

# 3. Посмотреть изменения
git log --oneline -5         # Последние 5 коммитов
git diff                     # Изменения в файлах
```

---

### Вариант 3: Автоматическая синхронизация

#### Настройте автоматический pull в VS Code:

1. Откройте настройки: `Ctrl + ,`
2. Найдите: `git.autofetch`
3. Включите эту опцию (галочка)

Теперь VS Code будет автоматически проверять обновления каждые несколько минут!

---

## 🔄 Полный Workflow для командной работы

### Каждый раз когда начинаете работу:

```powershell
# 1. Убедитесь что на правильной ветке
git branch

# 2. Подтяните последние изменения
git pull origin claude/ai-business-intelligence-agent-011CUvANvRyXyYWG58FytrG8

# 3. Проверьте статус
git status
```

### После получения обновлений:

```powershell
# Если обновлен requirements.txt:
pip install -r requirements.txt

# Если обновлены модели БД:
python migrate_db.py

# Если обновлен код:
# Перезапустите бота и Celery worker
```

---

## 🎯 Быстрая шпаргалка команд

| Действие | Команда Git | VS Code кнопка |
|----------|-------------|----------------|
| Скачать обновления | `git pull` | "Pull" / "Sync Changes" |
| Загрузить свои изменения | `git push` | "Push" / "Sync Changes" |
| Посмотреть историю | `git log` | Timeline (иконка часов) |
| Отменить изменения | `git checkout <file>` | Кнопка "Discard Changes" |
| Создать коммит | `git commit -m "..."` | Кнопка "Commit" |
| Посмотреть изменения | `git diff` | Source Control → файл |

---

## ⚠️ Частые ошибки и решения

### Ошибка: "Your local changes would be overwritten by merge"

**Решение**:
```powershell
# Вариант 1: Сохранить изменения
git stash
git pull
git stash pop

# Вариант 2: Отменить изменения (ОСТОРОЖНО!)
git reset --hard
git pull
```

### Ошибка: "CONFLICT (content): Merge conflict"

**Решение**:
1. Откройте конфликтующий файл в VS Code
2. Выберите нужную версию (кнопки в редакторе)
3. Сохраните файл
4. Выполните:
```powershell
git add .
git commit -m "Resolve merge conflict"
```

### Ошибка: "fatal: refusing to merge unrelated histories"

**Решение**:
```powershell
git pull origin <branch> --allow-unrelated-histories
```

---

## 🚀 Рекомендуемый процесс для вашего проекта

### 1. Утром (начало работы):
```powershell
# В VS Code терминале:
cd "C:\demo fiverr"
git pull origin claude/ai-business-intelligence-agent-011CUvANvRyXyYWG58FytrG8
pip install -r requirements.txt  # На случай новых зависимостей
```

### 2. В течение дня (после каждого обновления от меня):
```powershell
git pull origin claude/ai-business-intelligence-agent-011CUvANvRyXyYWG58FytrG8

# Проверьте что изменилось:
git log -1 --stat

# Если обновлен requirements.txt:
pip install -r requirements.txt

# Если обновлена БД:
python migrate_db.py
```

### 3. Когда делаете свои изменения:
```powershell
# Сохраните и закоммитьте:
git add .
git commit -m "Описание изменений"
git push origin claude/ai-business-intelligence-agent-011CUvANvRyXyYWG58FytrG8
```

---

## 💡 Полезные расширения VS Code для Git

1. **GitLens** - показывает кто и когда изменил каждую строку
2. **Git Graph** - визуализация истории коммитов
3. **Git History** - удобный просмотр истории

Установите через:
`Ctrl + Shift + X` → поиск → установить

---

## 📚 Дополнительные ресурсы

- [Git Handbook (GitHub)](https://guides.github.com/introduction/git-handbook/)
- [VS Code Git Tutorial](https://code.visualstudio.com/docs/sourcecontrol/overview)
- [Git Cheat Sheet](https://education.github.com/git-cheat-sheet-education.pdf)
