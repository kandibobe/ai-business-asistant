# run.py
import sys
import os

# Добавляем корневую папку проекта в системный путь Python.
# Это ГАРАНТИРУЕТ, что Python найдет наши модули (database, handlers).
project_root = os.path.abspath(os.path.dirname(__file__))
sys.path.insert(0, project_root)

# Теперь, когда путь настроен, мы можем безопасно импортировать.
from main import main

if __name__ == "__main__":
    print("🚀 Запускаем бота через правильную точку входа...")
    try:
        main()
    except KeyboardInterrupt:
        print("\n🛑 Бот остановлен пользователем.")
    except Exception as e:
        print(f"\n❌ Критическая ошибка во время работы бота: {e}")